{
  "private": true,
  "name": "allmyworker",
  "version": "1.0.0",
  "description": "A template for kick starting a Cloudflare Workers project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'",
    "build": "worktop build index.js"
  },
  "author": "ravindrasuthar",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2",
    "worktop.build": "^0.0.5"
  },
  "dependencies": {
    "faunadb": "^4.5.2",
    "worktop": "^0.7.3"
  }
}
